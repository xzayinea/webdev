# TODO

- [x] Inspect existing project auth pages and styling (`login.php`, `register.php`, `style.css`).
- [x] Improve login UI: consistent labels/inputs/role selector; keep existing backend logic.
- [x] Improve register UI: consistent labels/inputs; keep existing backend logic; role selection includes student/representative/executive/admin; student requires student_id as string.
- [x] Add small helper stylesheet for auth pages (`css/auth-fix.css`).
- [ ] Manual testing: load `login.php` and `register.php` in browser; verify validation + redirects work.
- [ ] Optional: check homepage `index.php` rendering if still blank.

